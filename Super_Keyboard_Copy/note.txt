
 file.imbue(std::locale("")); // 使用用户的默认locale
    if (file.is_open())
    {

编写函数f(x)，其功能是,。
编写函数g(x,y)，其功能是计算,。
在main()函数中输入四个实数a、b、c和d，调用g()和f()函数，计算并输出,（保留2位小数）。

输入输出格式：
scanf("%_%_%_%_",)
printf("%.2_",,)

 file.imbue(std::locale("")); // 使用用户的默认locale
    if (file.is_open())
    {

编写函数f(x)，其功能是,。
编写函数g(x,y)，其功能是计算,。
在main()函数中输入四个实数a、b、c和d，调用g()和f()函数，计算并输出,（保留2位小数）。

输入输出格式：
scanf("%_%_%_%_",)
printf("%.2_",,)